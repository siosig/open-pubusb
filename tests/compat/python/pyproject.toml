[project]
name = "open-pubusb-compat-python"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "google-cloud-pubsub>=2.27",
    "pytest>=8",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
